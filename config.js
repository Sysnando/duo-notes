// Supabase configuration. Committed on purpose: the anon key is public and
// Row Level Security is what protects the data. Leave all three empty to run
// the app in local-only mode (no login, localStorage persistence only).
window.DUO_CONFIG = {
  supabaseUrl: '',
  supabaseAnonKey: ''
};
